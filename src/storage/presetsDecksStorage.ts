// import type { PresetDeck } from "@/types/deck";

// const DECKS_KEY = "decks_mock_system";

// // 读取
// export const getStoredPresetsDecks = (): PresetDeck[] => {
//   const raw = localStorage.getItem(DECKS_KEY);
//   return raw ? (JSON.parse(raw) as PresetDeck[]) : [];
// };

// // 写入
// export const setStoredPresetsDecks = (decks: PresetDeck[]): void => {
//   localStorage.setItem(DECKS_KEY, JSON.stringify(decks));
// };
